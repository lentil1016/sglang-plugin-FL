# Ascend (Huawei NPU) Hardware Configuration
# This file defines CI/CD settings for Huawei Ascend NPU testing.

platform: ascend

# Docker image for this hardware
ci_image: harbor.baai.ac.cn/flagos-dev/sglang-plugin-fl:0.2.0-ascend-ci

# Runner labels for this hardware
runner_labels:
  - ci-sglang-plugin-fl-ascend

# Host paths mounted into the CI container.
# - /usr/local/Ascend/driver: NPU driver lives on the HOST, not in the CANN
#   toolkit image. Without it, npu-smi is missing AND torch_npu fails to load
#   its backend extension (driver/lib64 absent). Required for any NPU access.
#   check.sh auto-adds driver/tools to PATH so npu-smi runs with no image rebuild.
# Model cache + /usr/local/slog mounts are omitted on this runner (host paths not
# present); re-add per-runner if needed (e.g. for e2e model files).
container_volumes:
  - /usr/local/Ascend/driver:/usr/local/Ascend/driver

# Container options (--device for NPU access; --init reaps orphan zombies).
# NPU devices are passed via --device (there is no --gpus equivalent for Ascend);
# adjust the /dev/davinciN list to match the runner's actual NPU count.
# --user root is required: CI images install CANN/torch_npu libs under system paths,
# and pip install needs write access to /usr/local. This matches org convention.
# --init: tini as PID 1 reaps orphan zombies from sglang TP workers on shutdown
# (PID 1 was tail -f /dev/null, never wait()s -> zombies pile up, kill_process_tree times out)
container_options: >-
  --hostname sglang-plugin-fl
  --ipc=host
  --shm-size=64g
  --ulimit memlock=-1
  --ulimit stack=67108864
  --user root
  --device /dev/davinci0
  --device /dev/davinci1
  --device /dev/davinci2
  --device /dev/davinci3
  --device /dev/davinci_manager
  --device /dev/devmm_svm
  --device /dev/hisi_hdc
  --init
