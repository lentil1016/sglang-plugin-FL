# Ascend NPU Hardware Configuration
# This file defines CI/CD settings for Huawei Ascend NPU testing.

platform: ascend

# Docker image for this hardware
ci_image: harbor.baai.ac.cn/flagos-dev/sglang-plugin-fl:latest-ascend-ci

# Runner labels for this hardware
runner_labels:
  - self-hosted
  - Linux
  - ARM64
  - ascend
  - npu-8

# Container options (no --privileged; explicit --device covers all NPU access)
# Volumes are passed as -v flags here because GitHub Actions container.volumes
# does not support dynamic expressions.
container_options: >-
  --hostname sglang-plugin-fl
  --ipc=host
  --device /dev/davinci0
  --device /dev/davinci1
  --device /dev/davinci2
  --device /dev/davinci3
  --device /dev/davinci4
  --device /dev/davinci5
  --device /dev/davinci6
  --device /dev/davinci7
  --device /dev/davinci_manager
  --device /dev/devmm_svm
  --device /dev/hisi_hdc
  -v /usr/local/dcmi:/usr/local/dcmi
  -v /usr/local/bin/npu-smi:/usr/local/bin/npu-smi
  -v /usr/local/Ascend/driver/lib64/:/usr/local/Ascend/driver/lib64/
  -v /usr/local/Ascend/driver/version.info:/usr/local/Ascend/driver/version.info
  -v /usr/local/Ascend/driver:/usr/local/Ascend/driver
  -v /usr/local/sbin:/usr/local/sbin
  -v /etc/ascend_install.info:/etc/ascend_install.info
  -v /data:/data
